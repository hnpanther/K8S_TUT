apiVersion: v1
kind: Service
metadata:
  name: uiforward
  namespace: longhorn-system
spec:
  type: NodePort
  selector:
    app: longhorn-ui
  ports:
    - port: 8081
      targetPort: 8000
      nodePort: 30081

